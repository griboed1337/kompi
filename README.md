# PC Configurator

Мощный инструмент для сборки и конфигурации ПК.

## Возможности

- 🖥️ **Сборка ПК**: Интерактивный конфигуратор с проверкой совместимости
- 💾 **База данных**: Хранение продуктов в Supabase для быстрого доступа
- 🌐 **API**: REST API для получения и поиска продуктов
- 📱 **Адаптивный дизайн**: Работает на всех устройствах

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Supabase

1. Создайте проект на [supabase.com](https://supabase.com)
2. Выполните SQL из `schema.sql` в SQL Editor
3. Скопируйте API ключи в `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### 3. Запуск приложения

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## API Эндпоинты

### Получение продуктов
```bash
GET /api/products?q=RTX%204060&store=Citilink&page=1&limit=50
```

## Структура проекта

```
src/
├── app/
│   ├── api/
│   │   ├── products/route.ts    # API для продуктов
│   ├── build/                   # Страница сборки ПК
│   ├── browse/                  # Страница обзора
│   └── page.tsx                 # Главная страница
├── components/
│   └── ui/                      # UI компоненты
└── lib/
    ├── supabase.ts              # Supabase клиент
    ├── database.ts              # Функции БД
```

## База данных

### Схема
- `products` - таблица с продуктами
- `search_queries` - история поисковых запросов

### Индексы
- Поиск по названию (GIN индекс)
- Фильтрация по магазину
- Сортировка по дате

## Разработка

### Лицензия

MIT License

## Поддержка

Если возникнут проблемы:
1. Проверьте логи в Supabase Dashboard
2. Убедитесь в правильности API ключей
3. Проверьте RLS политики
4. Создайте issue в репозитории
